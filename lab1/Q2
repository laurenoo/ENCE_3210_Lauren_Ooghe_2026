#include <stdio.h>
#include <math.h>
#define PI 3.14159265358979323846


float cosTable[361];

int main() {
	for (int i = 0; i <= 360; i++) {
		double rad = i * PI/180.0;
		cosTable[i] = cos(rad);
	}
	for (int i=0; i <= 360; i++) {
		printf("cos(%3dB:) = %f\n", i, cosTable[i]);
	}

	return 0;

}
